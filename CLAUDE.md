# book-writing-ai — 매뉴얼

## 프로젝트 목적
작가의집 출판사를 위한 AI 책쓰기 코칭 플랫폼. Streamlit 본체(`app.py`) + Next.js 신버전(`book-coaching-next/`, `webapp/`) + 한국어 출판 워크플로 프롬프트(`prompts/`).

## 절대 룰 (NEVER / ALWAYS)
1. **NEVER** 결과물 파일을 프로젝트 폴더에 저장하지 말 것. **항상 `C:\Users\JUN\Downloads\`** 에 저장.
2. **NEVER** 원고 교정 시 **수정본 원고 파일**을 만들지 말 것. Before/After 리포트만. 단 **리포트 자체는 항상 `.md`+`.pdf`로 Downloads에 저장** — 채팅 텍스트로만 보고 금지, 묻지 말고 바로 생성.
3. **NEVER** 독자용 원고에 메타 텍스트(판본 노트·v번호·편집 요약박스) 금지. 말투는 "-습니다" 에세이 톤.
4. **NEVER** 홍보물에 수료증 관련 내용 포함 금지.
5. **ALWAYS** 보도자료는 `.docx`, 나머지 홍보물은 `.md`+`.pdf` 동시 생성.
6. **ALWAYS** 카드뉴스는 10장 이상, 에피소드+명문장+스토리 풍성하게.
7. **ALWAYS** 보도자료 리드 크레딧 기본값: `[북트립 이철화 기자]`.
8. **ALWAYS** 멀티 리포 환경 — 문서 보여드릴 때 현재 폴더 자동 판단 말고 **리포·브랜치·경로부터 확인**.
9. **NEVER** 다른 책을 소개·추천할 때 **제목만 보고 내용을 추론하지 말 것**. 반드시 그 책 원문(프롤로그·추천사·목차)을 열어 **주인공·장르·핵심주장**을 확인. 제목·정가는 **인쇄된 표지 PDF**, 쪽수는 **판권면**에서 확인(원고 파일명의 가제 사용 금지). 숫자("40가지"·"9년" 등)는 원문에서 세어 확인. → 2026-08-06 『고난 사전』 뒷날개 오인쇄 사고
10. **NEVER** 문자(SMS/LMS) 발송 본문에 특수기호 금지. **한글·숫자·영문·쉼표·마침표·괄호·하이픈(-)만** 사용. 금지: 가운뎃점 `·` / em·en대시 `— –` / 물결 `~` / 곡선따옴표 `" " ' '` / 화살표 `→` / 별표 `※` / 불릿 `•` / 콜론 `:`(1:1 → **1대1**) / 이모지 전부. 대체: `·`→쉼표, `—`→줄바꿈, `~`→"부터 까지". **이메일 본문은 해당 없음**(기호 사용 가능). → 2026-08-11 특수기호로 문자 발송 실패

## 검증 체크리스트 (작업 끝나면 스스로 점검)
- [ ] 결과물이 `Downloads/`에 저장됐는가?
- [ ] 홍보물이면 작가용 5종 / 출판사 내부용으로 분리됐는가?
- [ ] 원고 교정이면 수정본 원고 없이 Before/After만 보고했는가?
- [ ] **리포트를 `.md`+`.pdf` 파일로 만들어 Downloads에 저장했는가?** (채팅으로만 보고 금지)
- [ ] 맞춤법 요청이면 2-3라운드 반복 검토했는가?
- [ ] **문자 발송본이면 특수기호를 전부 제거했는가?** (룰10 — 한글·숫자·영문·쉼표·마침표·괄호·하이픈만)
- [ ] **뒷날개·추천도서면 룰9 6단계를 거쳤는가?** (마스터: `Downloads/작가의집_뒷날개_추천도서_홍보문안_모음/`)

## 트리거 단어 → 자동 동작
- **`/홍보물`** → 보도자료(.docx) + 카드뉴스(10장+, .md/.pdf) + 릴스 내레이션 영상 2개 + 작가용/출판사용 분리
- **`/교정`** → 1라운드(반복/구조) → 2라운드(비문/오탈자) → 3라운드(재채점) 자동, Before/After 리포트만
- **`/진단`** → 구조(25)+문장(25)+메시지(25)+몰입도(25) = 100점 채점. 목표 90점+
- **`/마케팅 <주제>`** → 3-에이전트 파이프라인: content-creator → sns-publisher → insight-analyzer
- **`/배포 <폴더>`** → Next.js→Vercel, 정적→Netlify 자동 판별 배포 + 200 응답 확인
- **`/표지 "<책 제목>" <키워드>`** → 표지 시안 3안(콘셉트·카피·헥스코드·디자이너 브리프)
- **`/제목 <주제>`** → 제목·부제 5안 + 교보·알라딘 동명도서 충돌 체크
- **`/목차 <원고경로 또는 키워드>`** → 8-12장 목차 초안 + 분량·흐름 균형 진단
- **`/리드 <책 정보>`** → 보도자료 리드 + 헤드라인 5안(자극·정보·감성·질문·숫자형)
- **`/리딩가이드 <책 정보>`** → 발제문 + 토론 질문 8개 + 워크시트 1장
- **`/출간판단 <원고경로>`** → 시장성·작가판매력·3대주제(돈·건강·관계) 100점 + 예상부수 + 출간/조건부/보류. `/진단`(원고가 좋은가)과 다름 — **팔리는가**만 봄
- **`/결제대조 <결제내역CSV>`** → 결제선생 내역과 발송이력 대조 → 안내 못 받은 결제자 추출. 취소·중복·소액 자동 제외
- **`/결제확인 <이름> <금액>`** → 결제 확인 문자·이메일 문안 생성. **룰10 특수기호 자동 제거**(URL만 예외)
- **`/아침`** → 미답장 상담메일·미안내 결제자·오늘 일정 → 오늘 할 일 5개
- **`/기획안 <통화메모 또는 원고>`** → 작가 제출용 출간기획안. 컨셉 3안(A/B/C) + 목차 + 기존원고 배치표
- **`/원고회신 <진단결과>`** → 작가님이 읽는 검토 회신문. `/진단`(내부 채점)과 다름 — 결론 먼저·원문 인용·소요시간 명시
- **`/상담준비 <이름>`** → 통화 **전** 브리프. 설문 원문 + 첫 문장 + 질문 5개 + 띄울 화면 3종 (`/상담복기`는 통화 후)
- **`/채널메시지 <원문>`** → 단톡방·문자·이메일 3버전 변환. 문자만 룰10 자동 적용
- **`/리드변환 <메타리드CSV>`** → 스티비 주소록 CSV 변환. 컬럼 정리 + 중복 제거 + 전화번호 정규화
- **`/원소스 <강의영상 또는 자막>`** → 강의 1개에서 전 채널 콘텐츠 일괄 생성(숏폼·블로그·카드뉴스·뉴스레터·원고꼭지)
- **`/숏폼선별 <강의자막>`** → 숏폼 후보 10구간 + 훅 문장 + CTA (Vrew 편집용)
- **`/자막교정 <SRT경로>`** → Vrew 자동 자막 오탈자·띄어쓰기 일괄 교정, 타임코드 보존
- **`/일간보고 [날짜]`** → 오늘 처리한 원고·홍보물 작업 KPI 일간 리포트
- **`/주간보고 [주차]`** → 지난 7일 KPI + 다음주 우선순위 제안

## 서브 에이전트 (`.claude/agents/`)
- **content-creator** — 블로그·인스타·유튜브 3종 동시 생성
- **sns-publisher** — 플랫폼별 포맷 변환 + 발행 예약
- **insight-analyzer** — 성과 분석 + 다음 주 5개 주제 제안
- **manuscript-doctor** — 100점 채점·Top 5 보강 포인트 (`/진단`에서 자동 호출)
- **proofreader** — 비문·오탈자·맞춤법 전담 (`/교정` 2라운드에서 자동 호출)
- **cover-designer** — 표지 콘셉트·카피·색감 (이미지 생성은 안 함, 디자이너 브리프)
- **book-researcher** — 교보·알라딘·국립중앙도서관 교차 검색 + 빈자리 분석
- **market-judge** — 시장성·작가판매력·예상부수 판정 (`/출간판단`에서 자동 호출). manuscript-doctor와 역할 다름
- **cardnews-creator** — 카드뉴스 10장 이상 생성, 에피소드+명문장+스토리 3요소 강제 (`/홍보물`에서 자동 호출)
- **press-writer** — 보도자료 `.docx` 생성, 리드 크레딧 기본값 적용 (`/홍보물`에서 자동 호출)
- **reels-narrator** — 내레이션 영상 스크립트 2종 생성 (`/홍보물`에서 자동 호출). 브루(VREW) 미사용
- **scoring-final** — 교정 완료 원고 최종 재채점 + 출간 가/불가 판정 (리뷰어=Opus)
원칙: 분신 남용 금지. 진짜 독립적 작업(자료조사·다중 콘텐츠)에만 사용.

## 사고 체크리스트 (명령 아님, 작업 전 읽을 것)
- **`.claude/commands/buffer_csv_체크리스트.md`** — 인스타 자동 발행 시 필독. → 2026-05-22 제주 시리즈 카드 2~9번 전부 차단(반복 URL + 해시태그 13개 매 카드 반복)

## 사용 도구
- Python 3.12 (`python` / `py`), Streamlit, FastAPI
- Node.js (Next.js 14, App Router, TypeScript, Tailwind)
- pandoc(docx 변환), python-docx, reportlab(PDF), pdftotext(추출)
- Git, Netlify(랜딩), Vercel(Next.js), Railway(FastAPI)

## 매뉴얼 업데이트 원칙
이 파일은 60줄 이내로 유지. 새 규칙은 직접 손대지 말고 "이 규칙 매뉴얼에 추가해줘"라고 지시할 것.
